server:
  port: 18800

spring:
  application:
    name: ai-answer

  datasource:
    url: jdbc:mysql://120.26.255.176:3306/ai_answer?useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_0900_ai_ci
    username: root
    password: "!Bond704747"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
    com.example.aianswer: debug

deepseek:
  # 在这里填你的 DeepSeek API Key，例如：sk-xxxxx
  api-key: "sk-97f0f9b89bf449a4bdc97d52891079ba"
  # 如无特殊需求，可使用默认 deepseek-chat
  model: deepseek-chat
  base-url: https://api.deepseek.com
  # 系统提示词（可按需调整）
  system-prompt: "你是中文医学问诊助手。只回答医学健康相关问题。回答不超过200字，简洁明确。若问题与医学无关或无法提供医学建议：请拒绝，并说明拒绝原因（不超过200字）。"
  # 后端兜底的最大字数（按字符截断）
  max-chars: 200

